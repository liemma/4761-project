anndata>=0.10
jupyter>=1.0
ipykernel>=6.29
scanpy>=1.10
python-igraph>=0.11
leidenalg>=0.10

# Allen Institute ABC atlas access (used in exploreAllen.ipynb / data download).
# The notebooks currently install from GitHub; keep it explicit here too.
abc_atlas_access[notebooks] @ git+https://github.com/alleninstitute/abc_atlas_access.git

